{
  "name": "guess-random-number",
  "version": "0.0.1",
  "description": "Guess random number game",
  "main": "build/index.js",
  "keywords": [],
  "devDependencies": {
    "@babel/core": "^7.18.10",
    "@babel/preset-env": "^7.18.10",
    "@babel/preset-typescript": "^7.18.6",
    "@stryker-mutator/typescript-checker": "^9.0.1",
    "@testcontainers/mariadb": "^11.11.0",
    "@types/jest": "^28.1.6",
    "@types/jest-when": "^3.5.5",
    "archunit": "^2.1.63",
    "babel-jest": "^28.1.3",
    "jest": "^28.1.3",
    "jest-when": "^3.7.0",
    "ts-jest": "^28.0.8",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  },
  "scripts": {
    "compile": "tsc --project tsconfig.json",
    "typecheck:test": "tsc --project tsconfig.test.json --noEmit",
    "test": "npm run typecheck:test && jest",
    "test:watch": "jest --watch",
    "test:coverage": "npm run typecheck:test && jest --coverage",
    "test:mutants": "npm run typecheck:test && stryker run"
  }
}
